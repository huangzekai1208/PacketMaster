# Web 报文附件发送计划

日期：2026-07-29

1. 在工作台维护会话级待发送报文，点击注册按钮直接打开浏览器文件选择器。
2. 选择文件后自动上传注册并返回输入区，移除报文选择弹窗与自动 `api.send` 调用。
3. 让普通会话发送请求同时携带文字和待发送 `capture_id`，并支持失败恢复。
4. 增加附件展示/移除样式和关键前端测试。
5. 运行前端测试、lint、类型检查和生产构建。
